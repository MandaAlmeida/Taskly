export type notificationProps = {
    _id: string,
    title: string,
    message: string,
    type: string,
    status: boolean,
    itemId: string,
    userId: string[],
    createdAt: string,
    updatedAt: string
}